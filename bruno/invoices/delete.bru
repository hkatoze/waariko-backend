meta {
  name: Delete Invoice (soft)
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/invoices/{{invoiceId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  x-company-id: {{companyId}}
}
