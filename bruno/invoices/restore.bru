meta {
  name: Restore Invoice
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/invoices/{{invoiceId}}/restore
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  x-company-id: {{companyId}}
}
