meta {
  name: Delete Client (soft)
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/clients/{{clientId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  x-company-id: {{companyId}}
}
