meta {
  name: Update Member Role
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/companies/{{companyId}}/members/{{userId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "role": "admin"
  }
}
